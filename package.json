{
  "name": "canvas-mcp-server",
  "version": "1.0.0",
  "description": "MCP Server for Canvas LMS - Provides Canvas data to AI assistants via Model Context Protocol",
  "repository": {
    "type": "git",
    "url": "https://github.com/jonasneves/canvas-mcp-server.git"
  },
  "scripts": {
    "build": "npm run build:extension && npm run build:native",
    "build:extension": "cd extension && zip -r ../dist/canvas-mcp-server-extension.zip . -x '*.git*' -x 'node_modules/*' -x 'generate-icons.html' -x 'icon-source.svg'",
    "build:native": "cd native-host && zip -r ../dist/canvas-mcp-server-native-host.dxt host.js manifest.json package.json package-lock.json",
    "release": "bash scripts/release.sh",
    "test": "echo \"No tests yet\"",
    "clean": "rm -rf dist"
  },
  "keywords": [
    "mcp",
    "canvas",
    "lms",
    "claude",
    "ai",
    "education"
  ],
  "author": "",
  "license": "MIT"
}
